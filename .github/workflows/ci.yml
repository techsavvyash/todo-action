name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: read
  issues: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run checks
        run: npm run all

  action-dry-run:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run action in dry-run mode
        uses: ./
        with:
          github-token: ${{ github.token }}
          dry-run: "true"
          dedupe: "false"

  action-issue-dogfood:
    runs-on: ubuntu-latest
    needs: action-dry-run
    if: github.event_name == 'push'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run action and create issues
        uses: ./
        with:
          github-token: ${{ github.token }}
          dry-run: "false"
          dedupe: "true"
